sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t tchen90pro/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true tchen90pro/react-test npm test

after_success:
  - docker build -t tchen90pro/multi-client ./client
  - docker build -t tchen90pro/multi-nginx ./nginx
  - docker build -t tchen90pro/multi-server ./server
  - docker build -t tchen90pro/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push tchen90pro/multi-client
  - docker push tchen90pro/multi-nginx
  - docker push tchen90pro/multi-server
  - docker push tchen90pro/multi-worker

deploy:
  provider: elasticbeanstalk
  region: "us-east-1"
  app: "multi-docker"
  env: "MultiDocker-env"
  bucket_name: "elasticbeanstalk-us-east-1-028156859150"
  bucket_path: "docker-multi"
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY